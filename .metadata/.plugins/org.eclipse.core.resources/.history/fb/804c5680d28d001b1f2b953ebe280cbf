#include <stdio.h>

int sumar(int numero 1, int numero 2);
int restar(int numero 1, int numero 2);

int main(){

	int num1;
	int num2;
	int resultado;
	pritnf("Ingrese un numero: ");
	scanf("%d",&num1);
	if(num1 > 100 || num1 < 10){
		printf("ERROR. Valor fuera de parametros: ");
		scanf("%d",&num1);
	}
	pritnf("Ingrese un numero: ");
	scanf("%d",&num2);
	if(num2 > 100 || num2 < 10){
		printf("ERROR. Valor fuera de parametros: ");
		scanf("%d",&num2);
	}

	return 0;

int sumar(int numero1, int numero2){

return 0;
}

int restar(){

return 0;
}

}
